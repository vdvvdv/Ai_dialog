name: ai_dialog
description: "Ai dialog: Kimi <-> DeepSeek"
publish_to: 'none'

# versionCode (число после '+') автоматически подменяется
# номером сборки в GitHub Actions — вручную не трогайте.
version: 1.0.1+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
