name: ai_dialog
description: "Ai dialog: Kimi <-> DeepSeek"
publish_to: 'none'

# versionCode и номер версии подменяются автоматически в GitHub Actions.
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
